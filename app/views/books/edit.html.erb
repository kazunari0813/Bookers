<h3>Editing Book</h3>

<%= form_for(@book) do |f| %>
	<h6>Title</h6>
	<%= f.text_field :title %>
	<h6>Body</h6>
	<%= f.text_area :body %>,<br>
	<%= f.submit 'Update Book' %><br>
	<%= link_to 'Show', book_path %>
	<%= link_to 'Back', books_path %>
	<% end %>